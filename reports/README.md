# Reports

Place **human-written** capstone deliverables here (Markdown, PDF, slides).

Automated CSVs, plots, and models from the CLI are written under `outputs/` (can be committed for review). Do not expect the pipeline to populate this `reports/` folder.
